# 示例文章

对你的 agent 说:

```
用 modern-tech 渲染 examples/sample-article/article.md
```

会在同目录生成 `article-formatted.html` (已 gitignore)。浏览器双击打开预览。换主题就把 `modern-tech` 改成 `modern-humanist` / `classic-tech` / `classic-humanist`。
