# 个人配置 (渲染时生效)

> 这个文件由**你的 AI agent 代填** —— 你只用自然语言说「署名用 XX」「招牌色用 #XXXXXX」, agent 会写进这里。一般不用手动编辑。

- signature: 作者          # 文末签名默认名; 每篇可用 frontmatter `signature: <名>` 覆盖, `signature: false` 关闭
- brand_accent:            # (可选) 你的招牌色 hex/rgb; 留空则用所选主题自带的强调色
