# Design Brief — inherited 家族（Claude Design 输入 A）

> 复制以下整段到 Claude Design **项目 A**，并上传 `style-dna-extracted.md` 内容（或原文截图）作为视觉锚点。

---

## 共通的项目背景（两份 brief 都用）

**项目**：个人公众号文章排版设计系统

**作者**：nyxx，CS 背景，写作内容是个人的思考、总结和观点

**定位**：公众号站在「**科技 + 人文**」、「**理性 + 感性**」的交汇点上。希望排版能同时承载两种气质：

- 数据复盘 / 理性分析 / 年度总结类长文 → **偏科技主题（tech）**
- 散文 / 回忆录 / 感性表达 → **偏人文主题（humanist）**

**通用技术约束（公众号渲染限制，两份 brief 都适用）**：

- ⛔ 所有样式必须 **inline**（写在 `style="..."` 里），**不能用 `<style>` 标签或 `class`**
- ⛔ **不能用 webfont**（不能引 Google Fonts、阿里云字体）——只能用系统字体栈
- ⛔ 不能用 `:hover` 等伪类
- ⛔ 不能用 CSS 变量 `var(--)`
- ⚠️ `flex` 简单布局 OK，复杂嵌套不稳；`grid` 不可靠
- ✅ `<section>` 嵌套常用且稳定（公众号编辑器原生输出就是这种）
- ✅ 内联 SVG OK
- ✅ 颜色用 `rgb()` 或 `#hex`

**通用产出格式（两份 brief 都按此格式产出）**：

为每个主题，输出**一段完整的 inline-styled HTML**，覆盖：

```
<section style="...">                  <!-- 根容器（line-height, color, font, padding） -->
  <h2 style="...">小节标题样例</h2>
  <p style="...">普通段落 <strong>含加粗</strong> 含 <em>斜体</em></p>
  <blockquote style="...">引用块样例</blockquote>
  <ul><li style="...">列表项</li></ul>
  <table style="..."><thead>...</thead><tbody>...</tbody></table>
  <!-- 7 个结构性 primitives 样例（见 brief 中清单）-->
</section>
```

**通用 primitives 清单（每个主题都需要给出 7 个 primitive 的对应版本）**：

1. `section-title` — 文章二级章节标题（H2）。视觉骨架，反复出现
2. `inline-emoji` — 段首装饰图标位置（emoji 也可改为线性图标）
3. `image-caption` — 配图说明，包含画名 · 作者 · 年份三段信息
4. `signature` — 文末个人签名/印章，建立公众号身份识别
5. `ai-disclosure` — 透明披露 AI 工具 + Prompt，存在感低
6. `quote-block` — 引用块
7. `sources` — 文末的「想法出处」块（**非正式参考文献**，谦逊语气）

**通用 markdown 元素清单**：`<p>` `<h1>~<h3>` `<strong>` `<em>` `<a>` `<ul>` `<ol>` `<li>` `<blockquote>` `<table>` 全套 `<code>` `<pre>` `<hr>` `<img>` —— 每个主题都需要全套样式覆盖。

---

## Design Brief — 「inherited」家族

**家族定位**：**继承 + 优化**。这是一次"工艺级精化"，不是重塑。需要保留作者原有公众号已经形成的视觉 DNA，让长期读者一眼就能认出"还是 nyxx 的号"，同时把每个组件的细节做到最优。

**必须保留的招牌元素（DNA）**：

- **蓝竖线小节标题** `| 文字` ——这是最具识别度的招牌符号，必须保留（颜色、字号、形态可微调，但"竖线 + 标题"的结构不可去掉）
- **AI 创作声明卡片** ——作者重视透明度，这是品牌承诺的一部分，必须保留（视觉可重做，但存在不可缺）
- **想法出处块** ——浅灰底卡 + 加宽字距 + 谦逊"*注"——保留这种"承认想法借鉴来源"的态度
- **段首 emoji 习惯** ——段落的视觉锚点，保留这种活泼性
- **结尾签名** ——文末个人收尾的仪式感，必须有
- **画作引用小标签** ——humanist 主题在用，保留这种文化引用气质
- **引用块（红线引用）** ——保留有线条装饰的引用风格

**两个主题的差异化**（在保留 DNA 基础上）：

- **`inherited-humanist`**：
  - 字体倾向衬线（标题用思源宋体）
  - 配色在保留招牌蓝的基础上加暖橘 / 雾蓝点缀
  - 段落节奏放慢、留白增加
  - 画作引用更书页感
- **`inherited-tech`**：
  - 字体严格无衬线
  - 配色克制（招牌蓝保留为重点强调色，主色用沉稳蓝或纯灰阶）
  - 增加数据卡片 / 对比块 / 表格美化（这些是新增组件，DNA 里没有，但贴合理性内容）
  - 信息密度更高

**你的任务**：

请基于上述 DNA，**在每个细节上做到最优**——字体的字距、卡片的圆角与阴影、装饰线的位置与粗细、配色的微妙比例、段落的节奏感、emoji 与图标的取舍等等。不需要新潮，需要**让现有风格的每个元素都达到设计专业水准**。

附作者原有公众号的 3 篇代表文章（详见 `style-dna-extracted.md`），是你的视觉锚点。
